# Archived mobile update PDFs

Older partner PDFs. Sources (markdown and config) are under [`../sources/archives/`](../sources/archives/).

Example: `mobile-update-v1.5.1+9.pdf` matches `RaphCare-v1.5.1+9.apk`.
